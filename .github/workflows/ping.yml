name: Ping Streamlit App

on:
  workflow_dispatch:
  schedule:
    - cron: '*/15 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping URL
        # Removed -L and added || true to prevent action failure
        run: curl -s -o /dev/null https://medical-rag-model-nexgfixgbyqbgui2crfde5.streamlit.app || true