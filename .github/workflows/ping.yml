name: Ping Streamlit App
on:
  schedule:
    - cron: '0 12 * * *' # Runs daily

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping URL
        run: curl -L -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64)" https://medical-rag-model-nexgfixgbyqbgui2crfde5.streamlit.app