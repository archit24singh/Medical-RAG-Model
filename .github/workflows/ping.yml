name: Ping Streamlit App

on:
  workflow_dispatch:
  schedule:
    - cron: '0 12 * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping URL
        # Notice the URL at the end of this line has changed
        run: curl -L -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64)" https://medical-rag-model-nexgfixgbyqbgui2crfde5.streamlit.app/_stcore/health