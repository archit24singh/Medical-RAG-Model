# Runtime dependencies for the Streamlit Community Cloud demo (pure-RAG mode).
# Pinned to the versions the baked chroma_index/ was built and tested with.
#
# The extra index provides CPU-only torch wheels — much smaller than the default
# CUDA build, which matters for the free-tier build size/memory budget.
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit>=1.40
chromadb==0.5.3
sentence-transformers==6.0.0
transformers==5.15.0
tokenizers==0.22.2
huggingface-hub==1.28.0
numpy==1.26.4
rank-bm25==0.2.2
openai==3.2.0
pydantic-settings==2.15.0
torch==2.13.0+cpu
